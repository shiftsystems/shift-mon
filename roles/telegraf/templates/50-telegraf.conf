{% if shiftmon_local_syslog_endpoint.startswith('udp') %}
$ActionQueueType LinkedList
$ActionQueueFileName srvrfwd
$ActionResumeRetryCount -1
$ActionQueueSaveOnShutdown on

*.* @127.0.0.1:{{ shiftmon_local_syslog_endpoint.split(':')[-1] }};RSYSLOG_SyslogProtocol23Format
{% endif %}
{% if shiftmon_local_syslog_endpoint.startswith('tcp') %}
action(type="omfwd" Protocol="tcp" TCP_Framing="octet-counted" Target="127.0.0.1" Port="{{ shiftmon_local_syslog_endpoint.split(':')[-1] }}" Template="RSYSLOG_SyslogProtocol23Format")
{% endif %}
