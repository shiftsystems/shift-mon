all:
  hosts:
    armin.local.shiftsystems.net:
  vars:
    syslog: true
    remote_syslog: false
    nginx: false
    loki:
      url: 'https://loki.example.com'
      retention_period: 90d
      #cert_path:
      #key_path:
    victoria:
      url: 'https://metrics.example.com'
      retention_period: 90d
      #cert_path:
      #key_path:
    ansible_python_interpreter: /usr/bin/python3
    grafana:
      domain: 'armin.local.shiftsystems.net'
      #cert_path:
      #key_path:
    email:
      enabled: true
      host: 'mail.example.com'
      alert_from_address: 'alerts@example.com'
      user: 'alerts@example.com'
      password: 'generate_this'
      alert_from_name: 'Shiftmon Alerts'
      port: '587'
    dns: # see Traefik's DNS challenge docs for the provider name and key values pairs for auth_vaules https://doc.traefik.io/traefik/https/acme/#providers
      provider: cloudflare
      auth_values:
        CF_DNS_API_TOKEN: 'Generate Me'
    users:
      telegraf: 'generate_this'
    ldap_host: 'dc1.example.com'
    ldap_port: '389' # use 636 for SSL use 389 for STARTTLS and please don't use plain text
    bind_dn: 'uid=grafana_bind,cn=users,cn=accounts,dc=example,dc=com'
    base_dn: 'dc=example,dc=com'
    bind_password: 'generate_this'
    user_search: '(\u0026(uid=%s)(memberOf=cn=ipausers,cn=groups,cn=accounts,dc=example,dc=com))'
    ldap_first_name: 'givenName'
    member_of: 'memberOf'
    ldap_last_name: 'sn'
    ldap_user: 'uid'
    ldap_email: 'mail'
    admin_group: 'cn=grafana-admins,cn=groups,cn=accounts,dc=local,dc=example,dc=com'
    viewer_group: 'cn=grafana-viewers,cn=groups,cn=accounts,dc=example,dc=com'
    editor_group: 'cn=server-editors,cn=groups,cn=accounts,dc=example,dc=com'
