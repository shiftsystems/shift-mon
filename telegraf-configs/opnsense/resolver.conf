[[inputs.tail]]
  name_override = "resolver"
  data_format = "grok"
  grok_patterns = ['''%{SYSLOGTIMESTAMP} %{GREEDYDATA:hostname} unbound\[%{INT}\]: \[%{INT:pid}:%{INT:thread}\] %{DATA}: %{DATA:resolver_ip} %{DATA:domain} %{DATA:record_type} %{DATA}''']
  files = ["/var/log/resolver/resolver_*"]